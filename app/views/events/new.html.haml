- content_for :title, "New event - "

%h2 Create new event
= render partial: 'record_errors', locals: { record: @event }

= form_for(@event, as: :event) do |f|
  = render partial: 'form', locals: {f: f}, object: @event

  = f.submit "Create"
  = link_to 'Cancel', events_path
